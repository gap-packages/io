sudo: false
dist: trusty
os:
  - linux
env:
  - TEST_SUITE=gap
  - TEST_SUITE=hpcgap
language: c
compiler:
  - gcc
  - clang
addons:
  apt_packages:
    - libgmp-dev
branches:
  only:
    - master
script:
  - ./run_tests.sh $TEST_SUITE
